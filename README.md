# AI Assistant с Долгосрочной Памятью (Mem0 + Qdrant)

## О проекте
Этот проект представляет собой интеллектуального чат-ассистента, способного запоминать контекст бесед, предпочтения пользователя и важные факты, сохраняя их между сессиями.
Система построена на интеграции **Mem0** (слой памяти), **Qdrant** (векторная база данных) и **OpenRouter** (LLM API).

## Особенности
*   🧠 **Долгосрочная память**: Автоматическое сохранение и извлечение воспоминаний.
*   💬 **Живой чат**: Консольный интерфейс для общения с ИИ в реальном времени.
*   🔍 **Семантический поиск**: Использование Qdrant для быстрого поиска релевантных воспоминаний.
*   🧹 **Управление памятью**: Специальные команды (`/reset`, `/clear`) для удаления истории общения.
*   ⚙️ **Гибкая конфигурация**: Настройка через файл `config.py` и переменные окружения.

## Требования
*   Python 3.10+
*   Docker (для запуска локального экземпляра Qdrant)
*   API ключ от OpenRouter (или OpenAI)

## Установка

1.  **Клонируйте репозиторий**:
    ```bash
    git clone https://github.com/your-repo/mem0-qdrant-chat.git
    cd mem0-qdrant-chat
    ```

2.  **Создайте и активируйте виртуальное окружение**:
    ```bash
    python -m venv .venv
    # Windows
    .venv\Scripts\activate
    # Linux/Mac
    source .venv/bin/activate
    ```

3.  **Установите зависимости**:
    ```bash
    pip install -r requirements.txt
    ```

4.  **Настройте переменные окружения**:
    Создайте файл `.env` в корне проекта и добавьте ваши ключи:
    ```env
    OPENAI_API_KEY=sk-or-v1-...  # Ваш ключ OpenRouter/OpenAI
    QDRANT_API_KEY=your_key      # Опционально (для локального Qdrant обычно не требуется)
    ```

## Запуск

1.  **Запустите Qdrant в Docker**:
    Убедитесь, что Docker запущен, и выполните команду:
    ```bash
    docker-compose up -d
    ```
    Это поднимет базу данных Qdrant, доступную по адресу `http://localhost:6333`.

2.  **Запустите чат**:
    ```bash
    python chat.py
    ```

## Использование
*   Просто пишите сообщения, чтобы общаться с ботом.
*   Бот будет автоматически запоминать важные детали из разговора.
*   При следующем запуске бот будет помнить контекст предыдущих бесед.

### Команды чата:
*   `/reset` или `/clear` — удалить все воспоминания текущего пользователя.
*   `exit` или `quit` — выйти из программы.

## Структура проекта
*   `chat.py` — Основной файл запуска чат-бота с интеграцией памяти.
*   `config.py` — Централизованная конфигурация Mem0 (LLM, Vector Store, Embedder).
*   `main.py` — Скрипт для демонстрации базовых функций (добавление/поиск) без интерфейса чата.
*   `docker-compose.yml` — Конфигурация для запуска Qdrant в контейнере.
*   `requirements.txt` — Список необходимых библиотек Python.
